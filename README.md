# Quiz Application

Современное приложение для проведения викторин с красивым анимированным интерфейсом, написанное на Python с использованием PyQt5.

## Содержание
- [Особенности](#особенности)
- [Требования](#требования)
- [Установка](#установка)
- [Структура проекта](#структура-проекта)
- [Описание папок и файлов](#описание-папок-и-файлов)
- [Как использовать](#как-использовать)
- [Архитектура](#архитектура)
- [Кастомизация](#кастомизация)
- [Известные проблемы](#известные-проблемы)
- [Запуск сервера и клиента](#запуск-сервера-и-клиента)

## Особенности

- 🎨 Современный анимированный интерфейс
- 🔐 Система авторизации с красивым градиентным фоном
- 📱 Адаптивный дизайн с фиксированными пропорциями
- 🎯 Интерактивные анимированные плитки для ответов
- 🏆 Система подсчета очков
- 🔄 Автоматическое переключение вопросов
- ✨ Визуальная обратная связь при ответах

## Требования

- Python 3.7+
- PyQt5
- Flask
- Операционная система: Windows/macOS/Linux

## Установка

1. Клонируйте репозиторий:
```bash
git clone https://github.com/yourusername/quiz-app.git
cd quiz-app
```

2. Создайте виртуальное окружение:
```bash
python -m venv venv
source venv/bin/activate  # для Linux/macOS
venv\Scripts\activate     # для Windows
```

3. Установите зависимости:
```bash
pip install -r requirements.txt
```

4. Запустите приложение:
```bash
python main.py
```

## Структура проекта

```
StudyProject/
├── main.py                 # Точка входа в приложение
├── requirements.txt        # Зависимости проекта
├── README.md               # Документация
├── backend/                # Backend часть приложения
│   ├── app.py              # Основной файл Flask-приложения
│   ├── quiz.db             # База данных SQLite
├── frontend/               # Frontend часть приложения
│   ├── api.py              # API для взаимодействия с backend
│   ├── controllers/        # Контроллеры
│   ├── models/             # Модели данных
│   ├── views/              # Интерфейс пользователя
```

## Описание папок и файлов

### Корневая директория
- `main.spec` - Конфигурационный файл для сборки проекта с помощью PyInstaller.
- `README.md` - Документация проекта.
- `requirements.txt` - Список зависимостей проекта.
- `start.sh` - Скрипт для запуска приложения на Unix-подобных системах.
- `StudyProject.spec` - Альтернативный конфигурационный файл для сборки.
- `icon.icns` - Иконка приложения для macOS.

### Папка `backend`
- `app.py` - Основной файл Flask-приложения, отвечающий за backend.
- `quiz.db` - База данных SQLite, содержащая вопросы и другую информацию.

### Папка `frontend`
- `__init__.py` - Указывает, что папка является модулем Python.
- `api.py` - API для взаимодействия с backend.
- `main.py` - Точка входа для запуска frontend.

#### Папка `controllers`
- `__init__.py` - Указывает, что папка является модулем Python.
- `main_controller.py` - Логика управления основным окном приложения.

#### Папка `models`
- `quiz_model.py` - Модель данных для викторины.
- `todo_model.py` - Модель данных для задач (если используется).

#### Папка `views`
- `auth_window.py` - Код для окна авторизации.
- `main_window.py` - Код для основного окна викторины.

### Папка `build`
- `StudyProject/` - Содержит скомпилированные файлы проекта, созданные PyInstaller.
  - `StudyProject` - Исполняемый файл приложения.
  - `warn-StudyProject.txt` - Лог предупреждений при сборке.
  - `xref-StudyProject.html` - HTML-отчет о зависимостях.

### Папка `__pycache__`
- Содержит скомпилированные файлы Python для ускорения выполнения.

## Как использовать

1. **Запуск приложения**
   - При запуске открывается окно авторизации
   - Введите логин и пароль
   - После авторизации автоматически запускается викторина

2. **Игровой процесс**
   - Читайте вопрос в верхней части экрана
   - Выбирайте ответ кликом по одной из четырех плиток
   - Правильный ответ подсвечивается зеленым
   - Неправильный ответ подсвечивается красным
   - За правильный ответ начисляется 10 очков
   - Следующий вопрос загружается автоматически

## Архитектура

Приложение построено по паттерну MVC (Model-View-Controller):

1. **Model (frontend/models/)**
   - Хранит вопросы и ответы
   - Управляет логикой подсчета очков
   - Отвечает за переключение вопросов

2. **View (frontend/views/)**
   - `auth_window.py`: Окно авторизации с анимированным градиентным фоном
   - `main_window.py`: Главное окно викторины с плитками ответов

3. **Controller (frontend/controllers/)**
   - Связывает Model и View
   - Управляет игровым процессом
   - Обрабатывает события пользователя

## Кастомизация

### Изменение вопросов
Отредактируйте метод `load_questions()` в `quiz_model.py`:
```python
def load_questions(self):
    self.questions = [
        {
            "question": "Ваш вопрос",
            "options": ["Вариант 1", "Вариант 2", "Вариант 3", "Вариант 4"],
            "correct_answer": 0  # Индекс правильного ответа
        },
        # Добавьте больше вопросов
    ]
```

### Настройка стилей
1. **Цвета плиток**
В `main_window.py` найдите массив `colors`:
```python
colors = ["#3498db", "#16a085", "#f39c12", "#e74c3c"]
```

2. **Градиент фона авторизации**
В `auth_window.py` найдите метод `paintEvent`:
```python
gradient.setColorAt(0, QColor(108, 92, 231))    # Фиолетовый
gradient.setColorAt(0.5, QColor(46, 134, 222))  # Синий
gradient.setColorAt(1, QColor(0, 206, 201))     # Бирюзовый
```

### Размеры окон
1. **Главное окно**
В `main_window.py`:
```python
self.setFixedSize(1040, 780)  # Ширина, Высота
```

2. **Окно авторизации**
В `auth_window.py`:
```python
self.setFixedSize(1040, 780)  # Ширина, Высота
```

## Известные проблемы

1. **Авторизация**
   - В текущей версии принимаются любые непустые логин и пароль
   - Для реальной авторизации необходимо реализовать backend

2. **Вопросы**
   - Вопросы захардкожены в коде
   - Для продакшена рекомендуется добавить загрузку из базы данных

3. **Состояние игры**
   - Не сохраняется между сессиями
   - Можно добавить сохранение прогресса в файл или БД

## Запуск сервера и клиента

### Запуск сервера
1. Перейдите в папку `backend`:
   ```bash
   cd backend
   ```
2. Убедитесь, что у вас установлен Python 3.7+ и все зависимости из `requirements.txt`.
3. Запустите сервер с помощью команды:
   ```bash
   python app.py
   ```
4. Сервер будет доступен по адресу `http://127.0.0.1:5000`.

### Запуск клиента
1. Перейдите в папку `frontend`:
   ```bash
   cd frontend
   ```
2. Убедитесь, что у вас установлен Python 3.7+ и все зависимости из `requirements.txt`.
3. Запустите клиент с помощью команды:
   ```bash
   python main.py
   ```
4. Откроется окно приложения с интерфейсом викторины.

## Лицензия

MIT License. См. файл LICENSE для деталей.